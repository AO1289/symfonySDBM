<header>
<nav class="navbar navbar-expand-lg sticky-top p-5">
	<div class="container-fluid d-flex justify-content-around">
		<a class="navbar-brand" href="{{ path('app_main') }}">SDBM</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse d-lg-flex justify-content-lg-end" id="navbarNav">
			<ul class="navbar-nav">
				<li class="nav-item">
					<a class="nav-link" aria-current="page" href="{{ path('app_main') }}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_article_index') }}">Gestion des bières</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_reference') }}">Gestion des références</a>
				</li>
				{% if app.user %}
					<li class="nav-item">
					<a class="nav-link" href="{{ path('profile_index') }}"><i class="fa-solid fa-user"></i></a>
				</li>
				{% else %}
					<li class="nav-item">
					<a class="nav-link" href="{{ path('app_login') }}"><i class="fa-solid fa-user"></i></a>
				</li>
				{% endif %}
				
				
			</ul>
		</div>
	</div>
</nav>
</header>