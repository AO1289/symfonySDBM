<form method="post" action="{{ path('app_fabricant_delete', {'id': fabricant.id}) }}" onsubmit="return confirm('Confirmer la suppression de cet élément');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fabricant.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
