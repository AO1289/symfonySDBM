{% extends 'base.html.twig' %}

{% block title %}Couleur - Société Des Bières du Monde{% endblock %}

{% block body %}
<main class="container vh-100">
    <h1 class="mb-5 mt-5">Couleur</h1>

    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ couleur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ couleur.nom }}</td>
            </tr>
        </tbody>
    </table>

   <a class="btn btn-primary mb-2 mt-3" href="{{ path('app_couleur_edit', {'id': couleur.id}) }}">Modifier</a>

    {{ include('couleur/_delete_form.html.twig') }}
 
    <a class="btn btn-warning mt-4" href="{{ path('app_couleur_index') }}">Retourner à la liste</a>
    
    </main>
{% endblock %}
